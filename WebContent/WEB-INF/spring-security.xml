<beans:beans xmlns="http://www.springframework.org/schema/security" xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd">

	<http auto-config="true">
		<form-login login-page="/login.html" username-parameter="j_username" password-parameter="j_password"
			authentication-failure-url="/login_error.html" />
		<intercept-url pattern="/" access="permitAll" />
		<intercept-url pattern="/modification/**" access="hasRole('ROLE_MODIF')" />
		<intercept-url pattern="/protected/**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
		<remember-me remember-me-parameter="remember-me" remember-me-cookie="remember-me" token-validity-seconds="1209600"
			data-source-ref="springDS" />
		<logout logout-success-url="/index.html" />
		<csrf disabled="true" />
	</http>

	<authentication-manager>
		<authentication-provider>
			<password-encoder hash="sha" />
			<jdbc-user-service data-source-ref="springDS" users-by-username-query="select email, password, true from USERS where email = ?"
				authorities-by-username-query="select email, role from USER_ROLES where email = ? " />
		</authentication-provider>
	</authentication-manager>

</beans:beans>
